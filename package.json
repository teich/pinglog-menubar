{
  "name": "pinglog-menubar",
  "version": "0.0.2",
  "description": "Menubar app for ping logging",
  "dependencies": {
    "menubar": "*",
    "request": "^2.61.0"
  },
  "devDependencies": {
    "electron-packager": "^5.0.1",
    "electron-prebuilt": "0.30.5"
  },
  "main": "main.js",
  "scripts": {
    "build": "electron-packager . PingLogger --platform=darwin --arch=x64 --version=0.30.5 --ignore=node_modules/electron --icon=Icon.icns",
    "start": "electron ."
  }
}
